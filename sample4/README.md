If you want to include files which include header files,
first make object files, then link them.  

### Note
**object file is machinery files but does not link and cannot execute.
if linked, it becomes executable file.**
